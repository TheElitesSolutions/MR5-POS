'use client';

import { useEffect } from 'react';
import { AdminApp } from '@/components/admin';

/**
 * Add-On Groups Management Page
 *
 * This page redirects to the comprehensive admin interface
 * that includes add-on groups management along with all other
 * admin functionality in a unified interface.
 */
export default function AddonGroupsManagementPage() {
  return <AdminApp />;
}
